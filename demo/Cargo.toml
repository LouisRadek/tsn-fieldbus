[package]
name = "demo"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
master = { path = "../master" }
slave = { path = "../slave" }

log = "0.4"
pnet = "0.35"
tokio = { version = "1.0", features = ["rt-multi-thread", "macros", "time"] }
tonic = "0.12"
libc = "0.2"
